inputs:
  test-case:
    description: String
    required: true
  test:
    description: String
    required: false
runs:
  using: composite
  steps:
  - uses: ./call-node
    with:
      test: ${{ inputs.test-case }}
      something: ${{ inputs.test }}
    env:
      INPUT_TEST: 7
