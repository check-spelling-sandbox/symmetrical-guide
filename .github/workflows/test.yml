on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./composite
        with:
          test: 1
      - uses: ./node
        with:
          test: 2
